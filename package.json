{
  "name": "greeting-app",
  "version": "1.0.0",
  "description": "A simple greeting application",
  "main": "src/app.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "clearMocks": true,
    "resetMocks": true,
    "restoreMocks": true
  }
}